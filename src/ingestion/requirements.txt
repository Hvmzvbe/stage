# ============================================================
# requirements.txt — ISO 8583 Parser Service
# Al Barid Bank — Plateforme MLOps Détection de Fraude
# ============================================================

# Core ISO 8583 parsing
pyiso8583==1.1.0

# Kafka client (Confluent — recommandé pour production)
confluent-kafka==2.4.0

# Environment variables
python-dotenv==1.0.1

# Testing
pytest==8.2.2
pytest-cov==5.0.0
